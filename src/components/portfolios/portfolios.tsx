import { FC } from 'react';
import { PortfoliosComponent } from './portfolios.styles';

const Portfolios: FC = () => {
  return <PortfoliosComponent>Portfolios</PortfoliosComponent>;
};

export default Portfolios;
